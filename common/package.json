{
  "name": "common",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "devDependencies": {
    "prettier": "^2.0.5",
    "typescript": "^3.8.3",
    "yarn": "^1.22.4"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "fmt": "prettier --write *.json src/**/*.ts",
    "install": "yarn clean && yarn build"
  }
}
